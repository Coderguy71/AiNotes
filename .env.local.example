GROQ_API_KEY=your_groq_api_key_here

# Classification API Configuration
# If GROQ_API_KEY is set, the classify API will use Groq's moonshotai/kimi-k2-instruct-0905 model.
# If not set, it will fall back to @xenova/transformers for local inference (requires more server resources).
